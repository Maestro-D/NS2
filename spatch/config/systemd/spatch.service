[Unit]
Description=an ssh proxy with access control list
Requires=network.target
After=network.target

[Service]
Type=simple
Restart=always
ExecStart=/usr/bin/spatch

[Install]
WantedBy=multi-user.target
